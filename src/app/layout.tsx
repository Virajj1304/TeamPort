import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TerraPulse — Real-time Soil Intelligence",
  description:
    "TerraPulse transforms raw sensor data into actionable soil health maps, helping farmers reduce water waste and increase yield predictability.",
  openGraph: {
    title: "TerraPulse — Real-time Soil Intelligence",
    description:
      "Live soil health maps & smart irrigation for precision agriculture.",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
